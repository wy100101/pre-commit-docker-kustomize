-   id: kustomize-multi
    name: kustomize-multi (via docker)
    description: kustomize provided by docker
    entry: kustomize-build-multi
    language: docker
    args: [overlay/production, 2]
    always_run: true
    pass_filenames: false
